#!/bin/rc

rfork ne

DISTFILE=http://distfile.com/distfile.tgz

. /sys/ports/Config/ports.conf

mkdir work ; cd work

# you usually will run mk install here
webfs ; hget $DISTFILE > work.tgz
tar xzvf work.tgz
rm work.tgz


This package provides the zlib library and headers for APE.

It is not needed for 9front since zlib is included in the distribution.  For other Plan9 distributions, it might be useful if ports are used.